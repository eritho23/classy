worker_processes 1;

events {}

http {
    server {
        listen 5555;

        location / {
            proxy_pass http://unix:./tmp/classy.sock:;
            proxy_http_version 1.1;
        }
    }
}
